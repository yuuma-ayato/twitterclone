<%= form_with(model: Tweet.new, local: true) do |form| %>
  <div class="blog_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to 'ツイート一覧画面に戻る', tweets_path %>
